# docker-compose.yml

services:
  fedorbot:
    build: .
    env_file:
      - .env
    volumes:
      - ./fedorbot.db:/app/fedorbot.db
      - ./logs:/app/logs
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"